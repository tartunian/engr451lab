function [sr,sc] = separate(s,fs)
    cutoff=1075/fs
    
    sr=[];
    sc=[];
    
    N = 1023;
    n = -(N-1)/2:(N-1)/2;
    d = dirac(n);
    
    lp = rectfilt(N,r_freq(1));
    hp = d - lp;
    
    
    
end

function w = freq2omega(f)
    w = 2*pi*f
end