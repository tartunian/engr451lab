function h = hammingfilt(N, wc)
n = (N - 1)/2;
M = -n:n;
h = wc/pi * sinc(wc * M );
w = 0.54 + 0.46 .* cos(2.*pi.*M/(N-1));
h = h .* w;
h = h/sum(h);
%     N = N+1-rem(N,2);
%     n = -(N-1)/2:(N-1)/2;
      h_ideal = wc/pi*sinc
%     w = (0.54+0.46*cos(2*pi*n/(N-1)));
end