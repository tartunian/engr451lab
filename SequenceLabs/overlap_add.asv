% lc - chunk length

function y = overlap_add(x,h,lc)
    xL = length(x);
    loop = ceil(xL/lc);
    x = zeros(1,lc*loop-xL);
    y = sequence(0,0);
    for i=1:loop
        temp=x(1+lc*(i-1):lc*i);
        tempoffset
end

